# Bind 0.0.0.0 to allow phones on the same LAN to reach the server.
HOST=0.0.0.0
PORT=8787

# Use a long random secret. Do not commit .env.
INGEST_TOKEN=replace-with-a-different-long-random-ingest-token
